<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Chat feature needs network. WAKE_LOCK + WIFI are used by the
         streaming locks in ChatViewModel; harmless if denied. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AskMeChat">

        <!--
            SplashActivity is the launcher. It uses `Theme.AskMeChat.Splash`
            whose windowBackground matches the splash content so there is
            zero flash between app start and the first composed frame.
        -->
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AskMeChat.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            MainActivity (chat screen) is no longer the launcher — the
            splash transitions into it. Still exported=false because
            nothing else on the device needs to start it directly.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AskMeChat"
            android:windowSoftInputMode="adjustNothing" />
    </application>

</manifest>
